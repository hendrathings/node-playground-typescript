{
  "name": "dropbox-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:dev": "npm run clean:build && webpack --config config/webpack.dev.js --bail --progress --profile",
    "build:prod": "npm run clean:build && webpack --config config/webpack.prod.js --bail --progress --profile",
    "clean:build": "rimraf build/",
    "start": "npm run build:dev"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "dotenv": "^5.0.1",
    "raw-loader": "^0.5.1",
    "rimraf": "^2.6.2",
    "ts-loader": "^4.2.0",
    "typescript": "^2.8.3",
    "webpack": "^4.6.0",
    "webpack-cli": "^2.0.14",
    "webpack-dev-middleware": "^3.1.2",
    "webpack-merge": "^4.1.2"
  }
}
